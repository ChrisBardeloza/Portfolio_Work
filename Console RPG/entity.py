#this will be a funtion for Monsters and Combat_classes to inherit

class entity:
    
    def deal_damage(entity):
        for index in range(self.dictionary):

            entity.dictionary(index[0])-= entity.dictionary(index[1]-index[2])